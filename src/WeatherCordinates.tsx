import React from 'react'
import { View, Text } from 'react-native'

interface Props {
  
}

const WeatherCordinates = (props: Props) => {
  return (
    <View>
      <Text>Weather Cordinates</Text>
    </View>
  )
}

export default WeatherCordinates
